class PrintArray
{
 public static void main(String args[])
 {
    int a[]={10,20,007,40,30};
    printArray(a,0,a.length);
 }
static void printArray(int a[],int i,int n)
 { 
    if(i>=n)
     return;
       System.out.print( a[i]+" "); 
        printArray(a,i+1,n);  
       i++; 
 }  
}
